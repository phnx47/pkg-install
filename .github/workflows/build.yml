name: Build Rust Project

on:
  push:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: ./publsih.sh

    - uses: actions/upload-artifact@v1
      with:
        name: artifacts
        path: target/publish
